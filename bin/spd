#!/usr/bin/env ruby

require 'distribute'

require 'common/logger'
require 'common/config'

# Load the configuration file
config = Common::Config.load_from_disk
if config.success?
  config = config.value
else
  log_fatal("Could not load configuration file: #{config.value}")
end

do_distribute config, ARGV
